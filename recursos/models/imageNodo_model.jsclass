itemMenuDebug = {
		Extends: itemMenu,
		click : function(pt) {
			console.log('nada');
		},
	
		init : function()
			{
				this.parent();
				var imgURL = this.options.mediaSrc;
				var imgID = "imageTest1"
				jQuery(this.DOM[2]).fancybox({
					'overlayColor' :'#000',
					'content':'<img src="'+imgURL+'">'
					});
				
				
			}
		
		
		
}